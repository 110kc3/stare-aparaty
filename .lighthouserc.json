{
  "_comment": "Lighthouse CI budget, run on pull requests by .github/workflows/ci.yml. Serves the assembled dist/ statically and audits the catalog plus one guide. Accessibility, best practices and SEO are hard gates because regressions there are always real defects; performance is a warning because a runner's timings are noisy and the catalog's image weight comes from OLX's CDN, which we do not control.",
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "url": [
        "http://localhost/index.html",
        "http://localhost/poradniki/lustrzanki-slr.html"
      ],
      "numberOfRuns": 3,
      "settings": {
        "preset": "desktop"
      }
    },
    "assert": {
      "assertions": {
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.95 }],
        "categories:performance": ["warn", { "minScore": 0.85 }],
        "cumulative-layout-shift": ["warn", { "maxNumericValue": 0.1 }],
        "total-byte-weight": ["warn", { "maxNumericValue": 2500000 }],
        "unused-css-rules": "off",
        "uses-long-cache-ttl": "off"
      }
    }
  }
}
