# TODO

Ideas to improve Stare Aparaty. Ordered roughly by impact-per-effort within each section. None of these are committed to — treat this as a backlog to cherry-pick from.

## Revenue & conversion

- [x] **Specific ASINs for the accessories section.** ~~The three links under "Akcesoria" still go to Amazon search pages.~~ Done — every product card links to a `/dp/{ASIN}` page.
- [x] **Track affiliate click-through.** ~~GoatCounter outbound events~~ Removed 2026-07-02 — replaced by Cloudflare Web Analytics (RUM, auto-injected). Note: Cloudflare doesn't support custom events, so per-link OLX/Amazon click tracking is gone.
- [x] **Show listing price on camera cards.** Done — `build-catalog.js` pulls price from OLX JSON-LD and renders a chip (cached in `olx_meta.json`).
- [ ] **Activate Google AdSense.** Code is done and shipping disabled — `scripts/ads-config.json` (`enabled: false`, empty `publisherId`) gates the loader tag, three ad units (mid-page, in-grid on the colour-film row, above-footer), and `ads.txt`; a generated `polityka-prywatnosci.html` is already live and linked from the footer. Remaining steps are all dashboard-side and written up in **`ADSENSE.md`**: (1) apply at adsense.google.com — note the real approval risk, a one-page affiliate catalog reads as thin content, and the fix if rejected is the guide pages below, not a resubmission; (2) set `publisherId` + `enabled: true`, rebuild, commit; (3) create the three display units and paste their `data-ad-slot` numbers into the config; (4) **configure the GDPR consent message** under Privacy & messaging → European regulations (Polish, reject-as-easy-as-accept, linked to the privacy page) — there is deliberately no hand-rolled banner in the repo because a self-built one isn't TCF-certified; (5) verify `ads.txt` resolves and the banner shows in incognito. ~~Also fill in the data-controller identity in `templates/privacy.template.html` §1 (business name / address / NIP).~~ ✅ **Settled 2026-08-07:** the site is run by a **private individual, not a registered business**, so §1 now names a natural person with `support@stareaparaty.com` as the contact channel — which satisfies RODO art. 13(1)(a-b) — and carries no company name, address or NIP. That absence is deliberate; do not "fix" it by inventing business details. If the site ever starts operating under a działalność gospodarcza, §1 needs that entity's name, address and NIP and `PRIVACY_UPDATED` needs a bump. Note this is separate from the *Allegro* konto firmowe: being able to invoice Allegro doesn't make a business the controller of this site's data. Revenue expectation at current traffic: negligible until the traffic work lands.
- [x] **Finish Allegro Affiliate Business onboarding.** Done — approval came through after the 2026-06-23 registration and the links are live. Evidence in the repo: all ten Allegro cards deep-link through `allegro.pl/affiliate?redirect_url=…` carrying `utm_medium=afiliacja`, `utm_source=ctr_b` and a single joined-campaign id `8902aaa9-6bd4-43f8-8111-28eb4c6a3dde`. **Still a recurring manual chore, not a finished one:** settlement is monthly accrual + a 45-day return-verification window, after which you issue a VAT invoice from Panel → Rozliczenia (net + VAT) quoting the Purchase Order number to `dostawcy.faktury@allegro.pl`. Attribution is 24h last-click (some blogs say 30 days — the official FAQ says 24h). Email `afiliacja@allegro.com` only if you later want server-to-server postback conversion tracking.
- [x] **Switch select product cards Amazon → Allegro (only where cheaper).** Done — the catalog now runs mixed, **10 Allegro cards to 5 Amazon**, and every edit this item called for is in place: the `href` and per-card CTA were swapped (10 × "Sprawdź na Allegro", 5 × "Sprawdź na Amazon"), the footer disclaimer reads "prowadzą do Amazon.pl **i Allegro.pl**", and prices moved into a sibling `scripts/allegro-products.json` (the per-marketplace split this item floated as an option) which `loadAllegroProducts()` feeds into both the price chips and the Product JSON-LD. The `LAST_REFRESHED` date is the *oldest* `lastChecked` across both marketplaces, so a stale Allegro price can't hide behind a fresh Amazon one. Category check done too: all ten Allegro links are `/produkt/` or `business.allegro.pl/oferta/` retail listings — no auction-format or "Kolekcja i Sztuka" listings, which pay no commission. The Amazon Associates `tag=blueprintkc0a-21` cards stay as the default where Amazon is competitive; a card moves to Allegro only where Allegro is genuinely cheaper. **When adding a card, edit `templates/index.template.html` — never `index.html`, which is generated — then rebuild with `node scripts/build-catalog.js`.** Two standing rules for any new card: keep it off auction-format and "Kolekcja i Sztuka" listings (Allegro pays no commission on either), and add its price to the matching `scripts/*-products.json` so the `LAST_REFRESHED` date stays honest.

## Discoverability / SEO

- [x] **Per-camera-type guide pages.** Done — five Polish buyer guides under `poradniki/` (SLR, kompaktowe, dalmierzowe, zestawy, obiektywy). Prose lives in `scripts/guides.json`, shell in `templates/guide.template.html`, rendered by `build-catalog.js`. Each guide auto-lists the cameras of its type live in the catalog (sold ones shown struck-out and unlinked), cross-links its siblings, links into the film sections, and carries `Article` JSON-LD + OG tags. The build refuses to run if a guide targets a type that `camera-types.json` doesn't define, so renaming a section can't orphan a guide. Homepage type headings link out to them; both workflows ship `poradniki/` via `cp -r`.
- [x] **Per-model guides — the two GROWTH-PLAN named.** Done — `poradniki/pentax-me.html` ("Pentax ME — recenzja i jaki film wybrać") and `poradniki/zenit-b-helios-44-2.html` ("Zenit B z Heliosem 44-2 — instrukcja dla początkujących"). `guides.json` now has two guide kinds: `type` guides own a catalog section and are what the homepage headings link to, `model` guides sit underneath one and pick their listings with a `match` keyword list. That keyword targeting is what makes them durable — the page keeps working when the specific body sells and a similar one arrives, instead of becoming a dead link. Model guides are deliberately excluded from the homepage headings (several share the SLR type and would fight over the one slot); they're reached from the cross-link row on every guide.
- [x] **More per-model guides.** Done — the three named candidates are live: `poradniki/praktica-bca.html`, `poradniki/yashica-35w.html`, `poradniki/canon-eos.html` (ten guides total). Each is built around the one thing a buyer of that model actually needs to know before paying: **Praktica BCA** — the PB bayonet is not M42, so a "body + lenses" bundle containing an older screw-mount Praktica is really two incompatible kits; **Yashica 35** — the 1.35 V mercury PX625 the meter was designed for is long discontinued, and an alkaline LR44 makes the meter read wrong (zinc-air cell, MR-9 adapter, or just meter with a phone); **Canon EOS** — the EF mount is still current so the lenses outlive the hobby, but EF-S glass physically cannot mount on a full-frame film body. Match keywords are `praktica`, `yashica 35`, `canon eos`. Note this vindicated the keyword design: the two Praktica listings classify as *Zestawy* and *Lustrzanki (SLR)* respectively, and the Canon EOS listing as *Zestawy* — no type guide could have gathered either set.
- [ ] **Further per-model guides — wait for data first.** The three above exhausted the pre-named candidates. Deliberately not guessing a fourth: hook up Search Console (GROWTH-PLAN action item 1), see which guide actually draws impressions, and write underneath that one. Adding an entry to `guides.json` with `kind: "model"` is a 20-minute job once there's a reason to pick a particular model.

- [x] **Open Graph + Twitter card meta.** Done — full OG/Twitter set + canonical + branded `og-image.png` in the template `<head>` (URLs now point to `stareaparaty.com`).
- [x] **Favicon + apple-touch-icon + web manifest.** Done — `favicon.svg`, `site.webmanifest`, and `theme-color` were already in place; now `scripts/generate-icons.js` rasterizes the pixel-art favicon into `apple-touch-icon.png` (180×180, linked in `<head>` for iOS) plus `icon-192.png` / `icon-512.png` (added to the manifest). All three ship in both workflows' `dist/` copy.
- [x] **`robots.txt` + `sitemap.xml`.** Done — both present, referencing the `stareaparaty.com` domain.
- [x] **Stop the sitemap lying about `<lastmod>`.** Done 2026-08-11 — this was a live defect, not a hypothetical. `renderSitemap` stamped *today* on the homepage and all ten guides on every nightly build, and the git log proves the claim was false: the 2026-08-08, -10 and -11 bot commits each touched `index.html` + `sitemap.xml` and **no guide file at all**, so ten of the twelve URLs advertised a modification that never happened. Google's sitemap docs are explicit that an implausible `lastmod` gets the element ignored outright — the old code was spending the signal to say nothing. Worse, the homepage's own daily diff was *only* its self-reported build stamp (`Aktualizacja: 11 sie 2026, 08:12`), so even the one honest-looking date was circular. Fix: each generated page is fingerprinted (SHA-256, with that build stamp stripped before hashing — otherwise the homepage reports today forever and the bug survives at the URL that matters most), and `scripts/page-state.json` stores per-URL `{fingerprint, lastmod}`. `<lastmod>` reports the stored date; it only advances when the fingerprint moves. Verified end-to-end by backdating the state file to 2026-07-01 and rebuilding: all twelve URLs kept the July date, and after corrupting one page's fingerprint only that URL advanced. The privacy policy now gets a real `<lastmod>` too (it previously got none — correctly, since a build-date one would have been false; it can have an honest one now). A URL with no recorded date emits no `<lastmod>` element rather than an invented one. The committed state file was **seeded from git history** (`git log -1 --format=%cs` per file) instead of stamping everything today, so the dates were honest from the first deploy — hence `kompaktowe.html` at 2026-08-09 and the rest at 2026-08-07. Workflow note: `scripts/page-state.json` is in the `git add` list in `discover-cameras.yml`, and that step now stages before checking `git diff --cached` — the old `git diff` check only saw tracked files, so a brand-new state file would have read as "no changes" and never been committed, leaving every run blind.
- [x] **Product JSON-LD.** Done — `build-catalog.js` emits a schema.org `Product`/`Offer` graph for the whole catalog (name, image, listing URL, used-condition, InStock/SoldOut, PLN price when known), injected via the `{{CAMERA_JSONLD}}` template placeholder.

## UX polish

- [x] **`loading="lazy"` on images.** Done — below-the-fold images lazy-load (first row stays eager for fast above-the-fold paint).
- [x] **Self-host the web fonts.** Done — `scripts/fetch-fonts.js` downloads Inter + Cormorant Garamond + Press Start 2P (latin + latin-ext, covering Polish) as `woff2` into `fonts/`, generates `fonts/fonts.css` with `font-display: swap`, and the template links that instead of `fonts.googleapis.com` (the preconnects + third-party render-blocking request are gone; `fonts/` ships in both workflows' `dist/`). Inter 400 latin is `<link rel="preload">`ed for the body text.
- [x] **Lightbox for camera photos.** Done — each camera card now has a magnifier button overlaid on the image (a sibling of the OLX link, so it neither navigates nor fires an outbound-click event) that opens an accessible lightbox (Escape / backdrop / close-button to dismiss, focus restored on close). The card itself still links to OLX.
- [x] **Respect `prefers-reduced-motion`.** Done — added a `@media (prefers-reduced-motion: reduce)` block that drops hover transforms, near-instant transitions, and smooth scrolling. (Note: the original `.retro` reference was the legacy page; the live template's `.product-card`/`.cam-card` hover transforms are what's now guarded.)
- [x] **Keyboard focus styles.** Done — explicit `:focus-visible` outline (2px solid `var(--gold)`, 2px offset) on links, buttons, and both card types.
- [x] **Group cameras by type (SLR / Rangefinder / Compact / Instant).** Done — `build-catalog.js` classifies each listing from its title (data-driven rules in `scripts/camera-types.json`; `^`-anchored keywords distinguish a standalone lens from a camera listed "z obiektywem") and renders a labelled sub-section per non-empty type, mirroring the film area's heading style. Falls back to a single flat grid when only one type is present. Auto-discovered cameras with no keyword match land in the SLR fallback (override in the config to retag).

## Build & workflow

- [ ] **Fix Amazon price scraper (currently disabled).** `scripts/refresh-amazon.js` works for the first product page after a warmup, then Amazon serves a captcha on every subsequent request — even with `--disable-blink-features=AutomationControlled`, patched webdriver/plugins/languages, a real viewport, and a ~10–18 s jittered delay between requests. The workflow file is parked at `.github/workflows/refresh-amazon.yml.disabled` so GitHub Actions skips it. Likely paths forward: (a) move to the Amazon Product Advertising API once Associates approves the account — the only ratelimit-friendly official channel; (b) route through a residential proxy pool (paid, but defeats per-IP fingerprinting); (c) try `playwright-extra` + `puppeteer-extra-plugin-stealth` for a stronger fingerprint patch than the in-house version; (d) drop the auto-refresh entirely and just keep a manual `node scripts/refresh-amazon.js` for ad-hoc local runs from a fresh IP. Until then, prices are hand-edited in `scripts/amazon-products.json` and the footer's "sprawdzone" date moves with each manual edit.
- [ ] **Hands-off price refresh via Amazon Product Advertising API (PA-API).** ⏸️ **Deliberately deferred 2026-08-07 — not blocked, declined for now.** The implementation below is fully specified and could be written today behind env-var gating, the way AdSense ships wired-but-disabled. Decided against it because the code could not be tested against the live API without keys (SigV4 request signing is unforgiving and silent-failure-prone), and because the Associates account itself is at risk under the <3-sales/180-days rule — so this could be a few hundred lines of untested, unrunnable code for an API that never turns on. The pressure behind it has also dropped: **10 of the 15 film/accessory cards now point at Allegro**, so Amazon price staleness affects a third of the catalog rather than all of it. Revisit if the Associates account survives and PA-API keys arrive; the spec below stays accurate. The fully unattended, captcha-proof, ToS-clean replacement for the disabled Playwright scraper — option (a) above, fleshed out. Prereq: an approved Amazon Associates account (the `blueprintkc0a-21` tag is already live on the site) plus a PA-API access key, secret key, and partner tag. Implementation: store `AMAZON_ACCESS_KEY` / `AMAZON_SECRET_KEY` / `AMAZON_PARTNER_TAG` as GitHub Actions secrets; rewrite `scripts/refresh-amazon.js` to call PA-API `GetItems` instead of scraping (ItemIds = the ASIN keys in `scripts/amazon-products.json`; Resources = `Offers.Listings.Price`, `Offers.Listings.Availability.Message`, `ItemInfo.Title`, `Images.Primary.Large`). Map the response back into `amazon-products.json` (price, lastChecked, image) and — crucially — when an item returns **no buy-box Offer** (the Fujifilm Acros `B085R9RN6F` failure mode), flag it for manual replacement instead of writing a stale price. Then rename `.github/workflows/refresh-amazon.yml.disabled` → `.yml` and let its weekly cron commit + push so Pages redeploys. Notes: `GetItems` batches up to 10 ASINs per call (the current catalogue fits in two calls), default throttle is ~1 request/s, and Amazon requires ≥3 qualifying sales every 180 days to keep API access alive. Until this lands, the weekly Cowork task `stare-aparaty-price-check` refreshes prices through a real browser session as the interim bridge.
- [x] **Ship a `CNAME` file with the custom-domain cutover.** Done — repo-root `CNAME` contains `stareaparaty.com` and is in the `cp … dist/` line of both `deploy-pages.yml` and `discover-cameras.yml`, so `actions/deploy-pages` can't clear the Settings-level custom domain on a deploy.
- [x] **Hardened the build/discovery fetches.** Done — both scrapers now use a 10s `AbortSignal.timeout` + one retry (a single hung OLX connection no longer stalls the whole run); `build-catalog.js` fetches listings with a bounded concurrency pool (5) instead of strictly sequentially; a transient **404** no longer flips a card to SPRZEDANE (only the definitive **410 Gone** does); and `discover-listings.js` falls back to an order-independent offer parser if OLX ever reorders its JSON keys (the old strict regex would otherwise return 0 and fail the run).
- [x] **Build-output guard.** Done — `build-catalog.js` refuses to write `index.html` if any `{{PLACEHOLDER}}` survives or the grid renders zero cards while links exist (`assertRenderedOutput`).
- [x] **Unit tests + CI.** Done — `test/` holds `node:test` suites for the script helpers (price/entity parsing, both OLX offer parsers, the output guard, the concurrency pool); `.github/workflows/ci.yml` runs `node --test` on PRs and pushes. No dependencies — built-ins only.
- [x] **Guard the links *between* the generated pages.** Done 2026-08-11 — `test/links.test.js`. The existing guards all check a page against *itself* (`assertNoPlaceholders`: were the placeholders filled; `assertRenderedOutput`: did any cards render), and none of them can see a link that points somewhere that no longer exists. That gap has a specific shape here: the site is generated from four templates, and `guide.template.html` hard-codes `../#filmy-bw`, `../#filmy-kolor` and `../#wywolywanie` — three ids owned by `index.template.html`. Renaming a homepage section id would silently break the same three links on **all ten** guide pages, with a green build and no warning. The new suite crawls the committed output the way a crawler would: it resolves all 202 internal `href`s across the 13 public pages, checks each target file exists and each `#fragment` matches a real `id`, asserts every `<loc>` in `sitemap.xml` points at a file that actually ships, and asserts each guide's `<link rel="canonical">` equals the path it was written to (a stale slug would otherwise tell Google to index a 404). Dependency-free, and it reads the generated files instead of rebuilding, so it never needs OLX to be up. **Verified by breaking it deliberately**, not just by watching it pass: renaming `id="wywolywanie"` produced eleven named failures (the homepage's own nav plus all ten guides), an invented `href` was caught as a missing file, a typo'd sitemap `loc` and a mismatched canonical each failed their own test. There is also a floor assertion on the number of links checked, so if the regex ever stops matching, the result is a failure rather than a silent "0 broken". Caveat worth knowing: it audits the *committed* HTML, so rebuild before running it after a template edit.
- [x] **Guard against a partial OLX scrape wiping the catalog.** Done — `discover-listings.js` already refused to write when a scrape returned *zero* offers, but a *partial* scrape (OLX serving page 1 and blocking page 2) passed that check and silently overwrote `product-links.txt` with a truncated list. This wasn't theoretical: the file went 19 → 10 links on 2026-08-06 and back to 19 the next night, so the site ran a half-empty catalog for a day. `shrinkRefusal()` now refuses any run that loses more than 30% of the previous list (floor of 5 links so first runs and small lists are exempt) and exits non-zero, which stops the workflow *before* the rebuild — the live site keeps the last complete catalog rather than publishing a partial one. `--allow-shrink` overrides it when listings really were removed in bulk. Threshold check against live data: 18 found vs 19 on file (one sold) passes at 5%; the observed 19 → 10 is refused at 47%.
- [x] **Validate discovered URLs in the workflow.** Done — `buildItem` now tags every listing with a `fetchStatus` (`ok` / `not-modified` / `sold` / `cached` / `placeholder`), and `reportFetchHealth` emits GitHub `::warning::` annotations so a degraded listing shows on the run page instead of only in the log. A *placeholder* card (fetch failed **and** no cached data — the case that actually reaches visitors as a broken entry) is reported separately from a plain cache fallback. Deliberately warn-only in the daily workflow: one dead OLX link must not block the deploy of the other 19 cameras. `node scripts/build-catalog.js --strict` turns placeholders into a non-zero exit for manual runs.
- [~] **Cache OLX fetches — implemented, but OLX doesn't cooperate.** Built the correct version: `olx_meta.json` now stores each listing's `etag`/`lastModified`, the next run sends `If-None-Match`/`If-Modified-Since`, and a `304` reuses the cached entry wholesale. Verified against live OLX: **it returns neither validator** (0/20 listings), so today this is inert — it costs nothing and starts working automatically if OLX ever adds them. The originally-proposed `actions/cache` keyed on the URL list was *rejected as wrong*: the daily run exists precisely to detect price and sold-state changes, so a time-based cache would serve stale data rather than save work. Only the origin can safely say "unchanged", which is what conditional requests ask.
- [x] **ESLint on `scripts/`.** Done — `eslint.config.mjs` (flat config), run in CI via `npx --yes eslint@9` so nothing is added to the dependency-free build path. Rules are correctness-only (`no-undef`, `no-unused-vars`, `use-isnan`, `eqeqeq`, `prefer-const`, …) with no formatting opinions. **Prettier deliberately skipped:** it would rewrite every hand-maintained file to match its defaults, producing a large diff that buries history while catching no bugs. Revisit only if more than one person starts editing `scripts/`.
- [x] **Lighthouse CI on pull requests.** Done — `.lighthouserc.json` + a `lighthouse` job in `ci.yml`, auditing the catalog and one guide page. Accessibility / best-practices / SEO are hard gates (a regression there is always a real defect); performance and CLS are warnings, since runner timings are noisy and the catalog's image weight comes from OLX's CDN, which we don't control. Audits the committed `index.html` rather than rebuilding, so the job doesn't depend on OLX being up.

## Accuracy of what the site says about itself

- [x] **Gate the ad *disclosures* on the ads flag, not just the ad markup.** Done 2026-08-11. `enabled: false` has always meant "no loader tag, no ad units, no third-party script" — but the prose describing ads was unconditional, so the site published statements that were simply untrue: the privacy policy §3 said *"Serwis wyświetla reklamy za pośrednictwem Google AdSense"* and *"Przy pierwszej wizycie zobaczysz komunikat zgody"* (no CMP is loaded, so no visitor has ever seen one), §2 listed *"Dane reklamowe — zbierane przez Google"*, §5 named consent under art. 6(1)(a) RODO as a basis for processing that wasn't happening, §8 said blocking cookies would only affect ad targeting (the site sets no cookies at all), the homepage footer said *"Strona wyświetla też reklamy Google."*, and `llms.txt` told agents *"The page carries Google AdSense display advertising."* A privacy policy is the wrong page to be inaccurate on even in the over-claiming direction, and it contradicted the property the site actually has — cookieless, no consent banner — which is the thing a reader opens §3 to check. Those blocks now switch on `ads-config.json` like the markup does. **The ads-on wording is the previously reviewed text kept verbatim** — verified by diffing an ads-on render against the published policy: the only differences are the loader script, the consent button and the date, so flipping `enabled: true` restores the document word for word. §3 keeps its heading in both states so §4–§9 don't renumber. `PRIVACY_UPDATED` bumped to 11 sierpnia 2026 (the published text genuinely changed), and `ADSENSE.md` now says to bump it again when the flag flips. Four tests pin both states, including one asserting all nine section numbers survive.
- [x] **Generate `llms.txt` so it can't drift.** Done 2026-08-11 — `templates/llms.template.txt` + `renderLlmsTxt`. Two separate drifts had already accumulated in a file that ships to the public site and is read by agents: the false ads claim above, and ten hand-copied guide lines that had to be edited in a second place every time a guide was added. Each guide now owns its English one-liner as an `llms` field in `guides.json`; the Pages section is built from it. The move was lossless — the regenerated file is byte-identical to the hand-maintained one apart from the three intended edits. A **test** enforces the field rather than the build: a missing blurb is a docs omission and must not be able to block the nightly deploy the way a broken type reference should, so `normalizeGuides` falls back to the Polish description and CI is what fails. Also corrected the agent-card line, which advertised only `/.well-known/agent.json` — the pre-0.3 path — while the deploy also serves the A2A 1.0 `/.well-known/agent-card.json` that a current client actually looks for; both are now listed. `llms.txt` and `llms-full.txt` were added to the workflow's `git add` list: `llms-full.txt` had been generated on every run but never committed, so the repo copy had been frozen while the deployed one moved.
- [x] **Fix the stale instruction in `ADSENSE.md`.** Done 2026-08-11. The "Before the first live impression" section still told the reader to add a business name, address and NIP to the privacy policy §1 and claimed the page said "właściciel serwisu" — both wrong since 2026-08-07, and actively harmful: it instructed exactly the invention of business details that three other files warn against. Replaced with the settled position (private individual, no NIP, deliberate), including the distinction from the Allegro konto firmowe. Also corrected "five guide pages" → ten, added the missing `guide` slot to the config example, and listed the full set of files a rebuild regenerates.

## Nice-to-have / someday

- [x] **Per-camera history blurb.** Done — `scripts/camera-notes.json` maps title keywords to a one-sentence note (era, origin, notable feature), rendered under the card title and clamped to two lines so a long note can't stretch its row. Same matching rules as `camera-types.json` (`^` anchors to the start of the title). All 20 current listings match a note; anything unmatched simply renders no line.
- [x] **"New arrivals" badge.** Done — `olx_meta.json` now stamps `firstSeen` on each listing and cards get a `NOWE` chip for 14 days (`NEW_ARRIVAL_DAYS`), placed opposite the price chip so the two never collide. Sold listings never show it. Listings that predate the field are backfilled as *not* new, so the first build after this change didn't stamp the whole catalog at once.
- [x] **Pixelate-on-hover effect.** Done — the card photo drops to chunky pixels behind the OLX pill on hover. CSS alone can't pixelate a full-resolution image, so this uses an inline `#pixelate` SVG filter (`feFlood`/`feTile`/`feMorphology`) applied on hover only; filtering all 20 images at rest would be needlessly expensive. Disabled under `prefers-reduced-motion`. To remove: delete the `.cam-card:hover .cam-card__img` filter and the `<svg>` block in the template.
